#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints the proper grammar, creating a piece of art.
 *              Followed by a new line.
 *
 * Return: Always 0 (indicating successful completion)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
